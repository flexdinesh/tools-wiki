# Plan: Docker Cheatsheet

## Target
`src/content/docs/cheatsheets/docker.md`

## Structure
1. **Install** - macOS/Colima setup
2. **Build** - build and tag images
3. **Registry** - login, pull, push
4. **Run** - run containers
5. **Containers** - ps, start, stop, rm, restart, logs, exec, cp, inspect
6. **Images** - images, rmi, explore image
7. **Cleanup** - prune commands + volume/network ls
8. **Compose** - up, down, logs, restart, exec
9. **Remote** - DOCKER_HOST
10. **Misc** - insecure registries

## Key Changes from Raw Content
- Add frontmatter (title, description)
- Add purpose line + example output for every command (per AGENTS.md format)
- Add missing commands: docker pull, docker cp, docker inspect, docker images, docker rmi, docker volume ls, docker network ls
- Consolidate stop-all/rm-all into proper docker stop and docker rm entries
- Move "explore image" into Images section
- Rename "Prune" → "Cleanup", fold volume ls/network ls in for context
- Remove redundant run example, empty login comment, inline flag explanations → purpose lines
- Consistent $ prompt in all bash blocks

## Decisions Made by User
- File goes in cheatsheets/ subdirectory
- Include docker cp, docker inspect, docker volume ls, docker network ls

## Verification
- Run dev server to confirm page renders
